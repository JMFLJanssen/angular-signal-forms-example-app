export enum status {
    Concept = 0 as number,
    Current = 1 as number,
}

export enum formStatus {
    New = 0 as number,
    Valid = 1 as number,
    Invalid = 2 as number,
}

export enum objectStatus {
    Unchanged = 0,
    New = 1,
    Updated = 2,
    ToBeDeleted = 3,
}
